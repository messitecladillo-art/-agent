# 团队守则（AGENTS.md）

> 在本仓库工作的所有 AI 成员（Qoder / Codex / Antigravity / Claude API 脚本）必须先读取并遵守本文件。
> 人类成员拥有最终决定权。AI 成员不得未经许可合并到 main、删除分支或修改本文件。

## 1. 成员与角色：发挥各自长处

| 成员 | 类型 | 长处 | 职责 |
|---|---|---|---|
| Qoder | 交互式代理 | 上下文编排、子代理调度、知识沉淀、长文写作 | **协调中枢**：拆解与分发任务、审查合并、问题分析、论文写作、跨阶段整合 |
| Codex | 云端自主编码 | 长时后台任务、自动化 PR 流程 | **编码工人**：算法实现、求解器代码、数据处理脚本、单元测试 |
| Antigravity | IDE 多代理 + 浏览器验证 | 端到端验证、前端/可视化 | **验证与展示**：可视化图表、结果展示页面、需要浏览器验证的演示 |
| Claude API 脚本 | 可编程代理（`scripts/agents/`） | 并行批量、无人值守 | **批量工 / 评审员 / 分析员**：批量文献处理、独立评审、定时结果分析 |

### 角色红线
- **评审与执行分离**：Codex / Antigravity 的产出，合并前至少经过一轮评审（`scripts/agents/review.py` 或 Qoder）
- **合并权集中**：只有协调者（Qoder）或人可以合并到 main
- **落盘才算数**：任何结论、发现、结果必须写进 `notes/` 或 `experiments/` 文件；对话里的口头结论无效
- **不越界**：不修改非自己所有权目录的内容；确有需要，先在 TASKS.md 提任务或留言

## 2. 目录所有权

| 目录 | 主责 | 其他成员的权限 |
|---|---|---|
| `models/` | Codex（公式→代码） | Qoder 可写模型说明文档 |
| `experiments/` | Codex | 所有人可读；`log.md` 只追加 |
| `paper/` | Qoder | Antigravity 供图到 `paper/figures/` |
| `viz/` | Antigravity | — |
| `scripts/agents/` | Qoder | — |
| `data/` | 任务负责人 | `raw/` 只读，不进仓库 |
| `notes/`、`TASKS.md` | 共享 | 按第 6 节规范追加/更新 |

## 3. 任务流程（TASKS.md 是唯一事实来源）

1. **拆解**：协调者把目标拆成带验收标准的任务，写入 TASKS.md「待认领」
2. **认领**：成员开工前先 `git pull`，把任务移到「进行中」并登记负责人与分支名，提交
3. **开发**：在分支 `task/<任务ID>-<简述>` 上工作
4. **完成**：把结果记入 `experiments/log.md`（代码任务须附最小自测通过证据），任务移到「待审」，提交并推送
5. **评审**：评审通过 → 合并到 main，任务标「已完成」；不通过 → 在任务上写明原因，退回「进行中」

## 4. 数学建模阶段分工

| 阶段 | 主责 | 辅助 | 产物落点 |
|---|---|---|---|
| 0 问题分析 | Qoder | — | 问题拆解 + 假设清单 → `notes/assumptions.md` |
| 1 建模 | Qoder | Codex 辅助公式代码化 | 模型描述 → `models/README.md` |
| 2 求解 | Codex | — | 代码 → `models/`，结果 → `experiments/` |
| 3 验证与灵敏度分析 | Codex | Qoder 审查 | `experiments/log.md` |
| 4 写作与图表 | Qoder | Antigravity 出图 | `paper/` |
| 5 提交前评审 | Claude API（review.py） | Qoder 终审 | `notes/reviews/` |

## 5. 提交与分支规范
- 分支：`task/<任务ID>-<简述>`，如 `task/T-03-ga-solver`
- 提交：`<任务ID> <动词>: 描述`，如 `T-03 实现: 遗传算法求解器`
- 绝不提交：`.env`、密钥、超过 10MB 的文件（大数据放 `data/raw/` 或云存储）
- 每次提交必须通过最小验证（脚本可运行 / 测试通过 / 文档可编译）

## 6. 同步与冲突规则
- 开工先读：`git pull`，读最新 TASKS.md 和相关笔记
- 干完就写：把「做了什么、结果如何、遗留问题」追加到对应笔记
- 不允许两个成员同时改同一个文件；冲突发生后，后提交方必须重读最新版再手动合并
- 任务卡住超过 30 分钟：在任务上记录阻塞原因，转做其他任务，不要静默空转

## 7. 安全底线
- 不将赛题、数据、论文草稿上传到约定的四个工具之外的任何第三方服务
- 密钥只放 `.env`（已被 gitignore），不进代码、不进提交信息

## 8. Skill 库（方法论层，`skills/`）
- AGENTS.md 管"谁、在哪、怎么协作"；`skills/` 管"每个阶段怎么做才对"，索引见 `skills/README.md`
- 六阶段各有一个 skill（审题破题/模型选型/求解实现/验证分析/论文写作/论文评审），外加范文精析与两条 workflow（比赛主流程、赛前冲刺）
- **派发任务必须指定 skill**：如"按 `skills/02-模型选型与建模.md` 完成子问题 2 的模型决策记录"
- **评审门不可省**：阶段产出过对应评审门才能进入下一阶段；`review.py` 已内置加载 `skills/06-论文评审与终检.md` 作为评审准则
- skill 的更新走正常任务流程：TASKS.md 登记、提交信息注明依据来源（年份与出处）

## 9. 议事厅前端补充

- `index.html`、`styles.css`、`app.js` 和 `assets/` 构成建模议事厅前端；它们只投影任务、Q、证据和审批状态，不替代文件黑板。
- 本地 `.collab/` 使用 `agent-collab/v1` 保存审查证据；该目录、`runtime/` 和本地资料缓存默认不提交。
- 字体资源位于 `assets/fonts/qingjia/`，许可证、修改声明和可重建源数据必须与字体一起保留。
- 本次融合由人类 Owner 明确授权推送；后续常规合并仍遵循“协调者或人类 Owner 批准后进入 main”的规则。
- 远端资料同步新增 `skills/08-美赛专项.md` 与 `notes/算法模板盘点.md`；它们与前端和本地事件源共享同一版本化黑板，不能被 UI 构建流程静默覆盖。
