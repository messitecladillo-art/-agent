 interface:
   display_name: "外部Agent证据重建"
   short_description: "静态还原步骤、输入输出、门与未决项"
   default_prompt: "Use $12-evidence-reconstruction to audit an external agent export."
 policy:
   allow_implicit_invocation: true
